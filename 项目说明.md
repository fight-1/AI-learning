# 项目说明

`fight-1 的 AI 学习笔记 · 活体宇宙` —— 一个把 Agent / MCP / LLM / Token 优化 / UI 设计 等 AI 学习资料沉淀成体系、并带「活体宇宙」视觉风格的个人技术笔记站。

## 这是什么

一个「内容即源码」的静态技术博客：每篇笔记都是仓库里 `src/content/notes/<分类>/<短名>.md` 的一个 Markdown 文件，写完 `git push` 就上线。
它用 Astro 5 静态输出全部页面，再叠加一层原生 Canvas / JS 的动效交互层（多皮肤主题、动态背景、配色工坊、雷达搜索、成就系统），
在保持纯静态、秒开、可离线的前提下，做出「宇宙飞船控制台」的观感。

**特色**（按辨识度排列）

- **5 套皮肤 × 多套背景动效**：星空 / 水墨 / 赛博 / 玄幻 / 极简，每套皮肤有 3~9 种背景渲染模式（星云、符文法阵、毛笔挥毫、古文古诗悬浮、矩阵雨…），可叠加组合。
- **配色工坊**：预设按 HSL 存色相、亮度随明暗自适应；也支持色轮自定义、🎲 随机配色、JSON 导入导出。
- **帧率自适应降级**：实时采样 FPS，掉帧自动逐级降粒子密度 / DPR / 关叠加层，恢复后自动升回，只影响当次会话。
- **零后端**：点赞、收藏、评论、设置全部走 `localStorage`；唯一的后端是访客计数的 1 个 Pages Function + KV。
- **PWA**：Service Worker 让首次访问后离线可读、回访秒开。
- **完整 SEO**：sitemap / robots / canonical / OG+Twitter / JSON-LD 结构化数据 / 每篇文章动态生成的 1200×630 分享图。

## 线上地址

| 项 | 地址 |
|---|---|
| 站点 | https://ai-learning-3wy.pages.dev/ |
| RSS 订阅源 | https://ai-learning-3wy.pages.dev/rss.xml （订阅说明见 `/subscribe`） |
| 源码 | https://github.com/fight-1/AI-learning |

站点地址配置在 `astro.config.mjs` 的 `SITE` 常量（影响 RSS 与站内绝对链接）；换自定义域名时改这一处即可。

## 技术栈

| 层 | 选型 |
|---|---|
| 框架 | [Astro 5](https://astro.build/)（静态输出，Content Collections + MDX） |
| 样式 | Tailwind CSS 3 + `@tailwindcss/typography` + 手写 `src/styles/global.css`（皮肤变量层） |
| 语言 | TypeScript（组件 frontmatter 与 schema 校验） |
| 部署 | Cloudflare Pages（推送即构建）+ Pages Functions + KV（`VISITOR_KV`） |
| 搜索 | [Pagefind](https://pagefind.app/)（构建期生成中文索引，纯静态、零后端） |
| 评论 | [giscus](https://giscus.app/)（GitHub Discussions，默认关闭、按需懒加载） |
| 动效 | 原生 Canvas 2D + 少量 GSAP / matter-js（仅卡片舞台与点赞物理反馈用） |
| 运行时 | Node 22（仓库已带 `.nvmrc` / `.node-version`） |

## 核心功能总览

**内容**
- 文章列表（可按 `?category=` / `?tag=` 筛选）、文章详情（自动生成目录、阅读时长、上一篇/下一篇、相关阅读）
- 归档（按年分组）、分类（固定书架）、标签（文字云 + 引力星轨两种视图）
- 全文搜索（Pagefind，支持 `/` 快捷键与 `?q=` 直达）
- 收藏夹、草稿舱（`draft: true` 的文章集中预览，已 `noindex`）
- RSS 全站源 + 按分类源（`/rss.xml?category=xxx`）、订阅说明页 `/subscribe`

**外观**
- 5 套皮肤：`cosmic` 星空 · `ink` 水墨 · `cyber` 赛博 · `xianxia` 玄幻 · `minimal` 极简
- 明暗三态：深色 / 浅色 / 跟随系统（`auto` 实时响应系统偏好变化）
- 配色工坊：预设 + 色轮 + 随机 + 导入/导出
- 专注阅读模式：隐藏背景动效、收起侧栏、正文加宽

**交互**
- 深空雷达搜索（📡 按钮，卡片按相关度飞入中心）
- 粒子力场（鼠标排斥 / 吸引）、点击爆发（涟漪 + 火花），均可开关
- 活体光标 + 粒子拖尾、注入能量（点赞）、牵引收藏、星际广播（本地树形评论）
- 卡片舞台（`/notes` 页 7 种布局变形）、首屏着陆动画、成就与彩蛋

**工程**
- 访客计数（PV / UV / 热门文章）、PWA 离线可读、`_headers` 缓存与安全头
- Cloudflare Web Analytics

## 目录结构

```
AI-learning/
├─ src/
│  ├─ content/
│  │  ├─ notes/<分类>/<短名>.md   # 文章源，Markdown / MDX
│  │  └─ config.ts                # Content Collection 与 frontmatter schema
│  ├─ pages/                      # 文件即路由（.astro 页面 + .ts/.js 端点）
│  ├─ components/                 # Nav、NoteCard、SkinSwitcher（战甲面板）、Toc
│  ├─ layouts/BaseLayout.astro    # 全站骨架：head/SEO/PWA/脚本入口/页脚
│  ├─ scripts/                    # 客户端脚本（背景引擎、交互、配色、雷达…）
│  ├─ styles/global.css           # 皮肤变量、排版、无障碍与打印样式
│  └─ lib/                        # notes.ts（内容读取）、poems.ts、universe.js（调度器）
├─ functions/api/visitors.js      # 唯一后端：Cloudflare Pages Function + KV 访客计数
├─ public/                        # 静态资源：favicon、manifest、sw.js、_headers、og/
├─ scripts/gen-og.py              # 本地生成文章 OG 分享图（Pillow）
├─ templates/note-template.md     # 文章模板（放这里不会被收录为文章）
└─ astro.config.mjs               # site、MDX/Tailwind 集成、Shiki 与 rehype 插件
```

## 快速开始

```bash
npm install        # 安装依赖
npm run dev        # 本地开发（http://localhost:4321）
npm run build      # 构建到 dist/，并生成 Pagefind 搜索索引
npm run preview    # 预览构建产物（含 Pages Function 之外的静态部分）
```

需要 **Node 22**（`.nvmrc` / `.node-version` 已指定）。

> 若本机 `npm run build` 被 safe-delete 守卫拦截而报 `exit 1`，改用：
> `env -u CODEBUDDY_SESSION_ID -u CLAUDE_SESSION_ID npm run build`

## npm scripts

| 命令 | 实际执行 | 说明 |
|---|---|---|
| `npm run dev` | `astro dev` | 启动开发服务器，改动热更新 |
| `npm run build` | `astro build && (pagefind --site dist \|\| true)` | 构建静态站点，再扫描 `dist/` 生成 Pagefind 索引；pagefind 失败不阻断构建 |
| `npm run preview` | `astro preview` | 本地预览 `dist/` 产物 |
| `npm run og` | `python scripts/gen-og.py` | 为所有非草稿文章生成 1200×630 的 OG 分享图到 `public/og/` |
| `npm run astro` | `astro` | Astro CLI 直通（如 `npm run astro check`） |

## 部署概述

托管在 **Cloudflare Pages**，推送到 `main` 即自动构建，约 1~2 分钟上线。

| 构建配置项 | 值 |
|---|---|
| Framework preset | `Astro` |
| Build command | `npm run build` |
| Build output directory | `dist` |
| 环境变量 | `NODE_VERSION` = `22` |

需要注意的两处外部配置：

1. **KV 绑定（访客计数）**：Pages 项目 → `Settings` → `Functions` → `KV variable bindings` → 变量名填 **`VISITOR_KV`**（与 `functions/api/visitors.js` 中的 `env.VISITOR_KV` 一致）。未绑定时计数器静默隐藏，不影响其它功能。
2. **`public/_headers`**：随产物一起发布，定义缓存策略与安全头（带 hash 的 `/_astro/*`、js/css/字体一年 immutable，图片 1 天，全站 `nosniff` / `Referrer-Policy` / `Permissions-Policy`）。为避免破坏大量内联脚本，未设置 CSP。

## 内容许可与作者

- **作者**：fight-1（GitHub [@fight-1](https://github.com/fight-1)）
- **源码许可**：仓库代码开源，见 GitHub 项目说明。
- **文章版权**：笔记内容归作者所有，转载请注明出处并保留原文链接。
- 站点为境外托管（Cloudflare Pages），无需 ICP 备案。
