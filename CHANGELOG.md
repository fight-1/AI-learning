# 更新日志

本文件记录**版本级**变更。站点内另有一份面向读者的「文章变动记录」页：`/changelog`（按文章最后变动时间倒序，区分新增 / 更新）。

## v1.1.0 — 2026-09-13

### 新增

- **最近浏览** `/history`：记录本机阅读足迹（最多 30 条），显示「刚刚 / N 分钟前 / N 天前」，可一键清空。
- **更新日志** `/changelog`：按最后变动时间倒序列出全部文章，标注「新增 / 更新」。
- **系列已读进度**：`/series` 每个系列显示进度条与「N / M 已读」，读过的篇目带 ✓。
- **阅读主题预设**：控制台可切换 纸书 / 羊皮卷 / 护眼，只改变正文区配色，不影响导航与全站外观。
- **快捷键自定义**：控制台可修改「专注阅读（默认 `F`）/ 切换主题（`T`）/ 回到顶部（`G`）」的键位，`?` 面板会实时显示改动。
- **移动端 / 平板导航**：≤860px 启用汉堡抽屉 + 底部快捷栏（首页 / 笔记 / 搜索 / 设置）；设置面板内可直接换主题、唤起雷达、展开战甲（皮肤 / 背景 / 配色以底部面板形式打开）。
- **笔记舞台新增「列表」模式**：与网格 / 扇形 / 纵深 等并列为同级模式，列表为「一行一个」的横向信息带。

### 改进

- 卡片操作按钮：能量图标 ⚛ → ⚡，收藏按钮拆分为独立图标与文字，不再与全局样式冲突。
- 多个功能页（归档 / 分类 / 系列 / 摘录 / 图谱 / 搜索 / 收藏 / 标签）的标题区、视图条、筛选项、空状态统一为共享组件样式。
- 知识图谱改为**连通分量布局**：互不相连的标签簇不再挤成一团，而是按分量分槽铺开并整体居中。

### 修复

- 收藏页「网格 / 列表」切换无效：`setView()` 只改了 `data-view`，未同步切换 CSS 类。
- 列表视图样式不生效：列表项由 JS 动态注入，不带 Astro 作用域类，原先写在页面作用域内的规则无法命中（已移至全局样式）。
- 文章页内链：修正为站内绝对路径，避免相对路径因目录层级多算一级而 404。

### 工程

- 不再提交 `package-lock.json`（已加入 `.gitignore`）：Cloudflare Pages 检测到 lock 会跑 `npm ci`，lock 与 `package.json` 失同步会导致构建失败。
- CI 工作流移除 `astro:check` 步骤（仓库存在大量历史类型告警，而 `astro build` 本身不做类型检查）。
- 新增 `src/scripts/history.js` 统一管理阅读足迹。

## v1.0.0

首个正式版本：内容管线（Content Collections + 双链 + 系列 + 摘录 + Mermaid）、5 套皮肤与配色工坊、
背景动效引擎（FPS 自适应降级）、Pagefind 搜索与分面筛选、知识图谱、舰长控制台、PWA、SEO 与 OG 图、
Cloudflare Pages 部署与 KV 访客计数。
