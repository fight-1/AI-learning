# 架构设计

本文描述 `fight-1 的 AI 学习笔记` 的当前架构：一个「静态优先」的站点，构建期生成全部 HTML，运行时只有少量客户端脚本与 1 个 Cloudflare Pages Function。

## 一、总体架构

**原则**：能在构建期做完的事不做在运行时；必须运行时做的事控制在「一个 Function + 若干 Canvas 脚本」。

```
构建期（Astro SSG）
  src/content/notes/**/*.md  ──►  Content Collections（schema 校验）
                                     │
                                     ├─► src/pages/**/*.astro  ──► 静态 HTML（含 SEO/JSON-LD）
                                     ├─► 端点：sitemap.xml / robots.txt / rss.xml
                                     │       search.json / articles.json / tags-graph.json
                                     └─► pagefind --site dist   ──► /pagefind/* 搜索索引

运行期
  浏览器 ──► 静态 HTML（CDN 边缘缓存）
          ├─► <canvas id="bg"> 背景引擎（bg.js，单 rAF 循环）
          ├─► 客户端脚本层（配色/光标/音效/雷达/收藏/访客上报…）
          ├─► Service Worker（public/sw.js，离线可读）
          └─► /api/visitors  ──►  Pages Function  ──►  Cloudflare KV（VISITOR_KV）
```

**请求/渲染流程**

1. 首次请求命中 Cloudflare Pages 的静态产物，`BaseLayout.astro` 头部有一段 `is:inline` 脚本，在解析 CSS 之前就从 `localStorage` 读出 `skin` / `theme` / `focus` / `bg` 写到 `<html>` 的 `data-*` 属性，避免主题闪烁（FOUC）。
2. 页面加载后，`BaseLayout` 的模块脚本统一 import 客户端脚本层并初始化（见第五节）。
3. 背景引擎读取 `data-skin` + `data-bg` 决定渲染器，并注册 `MutationObserver` 监听这几个属性变化以重建渲染器。
4. `load` 事件后 `visit-counter.js` 上报一次 PV（携带当前 path），页脚回填「访客 N · 浏览 M」。
5. 若浏览器支持，`sw.js` 在 `load` 后注册，接管后续静态资源与导航请求。

## 二、内容管线

### Content Collection

`src/content/config.ts` 用 `glob({ pattern: '**/*.{md,mdx}', base: './src/content/notes' })` 收集文章，并用 Zod 校验 frontmatter：

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `title` | `string` | 必填 | 标题，用于卡片、OG 图、JSON-LD |
| `date` | `coerce.date()` | 必填 | 发布日期，用于排序与归档 |
| `category` | `string` | 必填 | 分类（固定书架），如 `Agent` / `MCP` / `Token 优化` |
| `tags` | `string[]` | `[]` | 标签（自由贴纸），用于标签页与相关阅读打分 |
| `summary` | `string` | `''` | 一句话摘要，卡片、RSS、OG meta 均使用 |
| `cover` | `string?` | — | 可选封面；OG 图缺失时作为二级回退 |
| `draft` | `boolean` | `false` | `true` 不发布（列表 / RSS / sitemap 全部过滤） |
| `featured` | `boolean` | `false` | `true` 卡片显示「精选」角标 |

文章 ID = 相对 `notes/` 的路径去掉扩展名，如 `agent/multi-agent-orchestration`，对应路由 `/notes/agent/multi-agent-orchestration`。

### `src/lib/notes.ts`

| 导出 | 职责 |
|---|---|
| `getNotes()` | `getCollection('notes', ({data}) => !data.draft)`，按日期倒序 |
| `formatDate(d)` | `toISOString().slice(0,10)` |
| `readingTime(text)` | 中文按 350 字/分钟、其余按 200 词/分钟估算，最少 1 分钟 |
| `uniqueCategories(notes)` | 去重分类 |
| `uniqueTags(notes)` | 扁平化 + 去重 + 排序 |

草稿另有一条路径：`src/pages/drafts.astro` 用 `getCollection('notes', ({data}) => data.draft)` 单独取出，页面带 `noindex`。

### 构建期端点

| 端点 | 产物 | 说明 |
|---|---|---|
| `sitemap.xml.ts` | `/sitemap.xml` | 自生成（不依赖 `@astrojs/sitemap`），只收录 `'' /notes /categories /tags /subscribe /about /search` 与全部非草稿文章 |
| `robots.txt.ts` | `/robots.txt` | `Allow: /` + Sitemap 地址 |
| `rss.xml.js` | `/rss.xml` | 支持 `?category=xxx` 生成分类源；不注入 XSL 样式表（现代 Chrome/Edge 已移除 XSLT） |
| `search.json.ts` | `/search.json` | 轻量索引（id/title/summary/category/tags/url/date），供雷达搜索与收藏页 |
| `articles.json.ts` | `/articles.json` | 路径→标题映射，供首页「热门文章」把 KV 的 PV 数据 join 成标题 |
| `tags-graph.json.ts` | `/tags-graph.json` | 标签共现图（节点=标签+热度，边=同篇共现次数），供引力星轨 |

### rehype 插件

`astro.config.mjs` 里手写了一个 `rehypeCodeMeta()`：把围栏语言写到 `<pre data-language>`、把 ```` ```ts title="src/main.ts" ```` 的 `title` 写到 `<pre data-title>`，零新增依赖；前端 `fx.js` 据此渲染代码块头部（语言标签 + 文件名 + 复制按钮）。

## 三、主题系统

主题由 `<html>` 上的两个属性驱动，全部样式在 `src/styles/global.css` 中以属性选择器定义：

| 属性 | 取值 | 持久化 |
|---|---|---|
| `data-skin` | `cosmic` · `ink` · `cyber` · `xianxia` · `minimal` | `localStorage.skin` |
| `data-theme` | `dark` · `light`（`auto` 在写入前解析为二者之一） | `localStorage.theme` |

- **自动主题**：`theme` 为空或 `auto` 时按 `prefers-color-scheme: light` 解析；并监听该 media query 的 `change` 事件实时跟随系统。
- **切换入口**：导航栏 ☾/🖥/☀ 按钮（dark → light → auto 循环）、战甲面板、`/console` 控制台。
- **默认背景**：首次进入（无 `localStorage.bg`）时按「皮肤 × 明暗」查表选最契合的模式，避免开场不匹配。

### 配色系统（`src/scripts/palette.js`）

作用变量：`--accent`（主色）、`--accent-2`（副色）、`--glow`（辉光）。

| 键 | 内容 |
|---|---|
| `localStorage.palette` | 当前预设 id：`default` / `forest` / `ocean` / `amber` / `crimson` / `violet` / `custom` |
| `localStorage.palette_custom` | 自定义三色 JSON：`{accent, accent2, glow}` |

- **预设只存色相 H 与饱和度 S**，亮度 L 由 `data-theme` 自适应：深色模式取较亮（0.62）、浅色模式压暗（0.40），因此同一预设在任意皮肤与明暗下都保持协调与可读。
- **副色自动派生**：主色色相 +30°、饱和度 ×0.95、亮度按明暗微调，保证主副可区分。
- **优先级**：自定义 > 预设 > 皮肤自带（`currentColors()` 在「跟随皮肤」时直接读 computed 真实值，避免与 CSS 两处漂移）。
- **能力**：预设切换、色轮实时预览（`input` 只写样式、`change` 才落盘）、🎲 随机配色（随机 H + 0.55~0.85 饱和度）、⤓ 导出 `palette.json`、⤒ 导入（接受 `{custom:{...}}` 或裸 `{accent,...}`）、↺ 复位。
- 监听 `data-theme` 变化，预设亮度会重新自适应。

## 四、背景动效架构（`src/scripts/bg.js`）

### 单循环 + 注册表

- 全站只有一个 `requestAnimationFrame` 循环。循环内依次执行：`sampleFps(dt)` → `current.frame(t, dt)` → 力场叠加帧 → 点击爆发帧。
- 渲染器以 `REG` 注册表按皮肤分组，`<html data-bg>` 决定当前模式；`data-bg` 支持 **`"A+B"` 组合**，用 `compose()` 叠加渲染两层（例如 `star+nebula`）。

| 皮肤 | 可用模式（注册表 id） |
|---|---|
| `cosmic` | `star` `galaxy` `meteor` `pixel` `constellation` `nebula` `float` |
| `cyber` | `grid` `matrix` `holo` `circuit` `hex` `float` |
| `xianxia` | `qi` `rune` `lotus` `poem` `xiangyun` `constellation` `guwen` |
| `ink` | `ink` `landscape` `petals` `inkdot` `brush` `xiangyun` `poem` `yinzhang` `guwen` |
| `minimal` | `dust` `line` `float` |

各皮肤默认模式：`cosmic→constellation`、`cyber→matrix`、`xianxia→rune`、`ink→brush`、`minimal→float`。
新增背景需要同步三处：`REG` 注册、默认模式表、以及 `SkinSwitcher.astro` / `console.astro` 的按钮清单。

- **叠加层（fx）**：`data-bg-force` 控制粒子力场（`repel` 排斥 / `attract` 吸引 / `auto` / `off`），`data-bg-click` 控制点击爆发（`on` / `off`）。指针事件全局绑定一次，触屏走 `touchmove/touchstart`。
- **配色联动**：粒子颜色从 computed 读取 `--accent` / `--accent-2` / `--bg`，因此换肤与配色工坊会即时反映到背景。

### FPS 自适应降级（perf 状态机）

每约 1 秒采样一次平均 FPS，带滞回地在 L0~L3 之间升降：

| 级别 | 粒子系数 `PART_FACTOR` | DPR 上限 `DPR_CAP` | 叠加层 |
|---|---|---|---|
| L0 | 1.0 | 2 | 开 |
| L1 | 0.6 | 2 | 开 |
| L2 | 0.4 | 1 | 开 |
| L3 | 0.4 | 1 | **关**（力场 + 点击爆发） |

- **降级**：连续 2 个采样窗口 `< 50fps` → 升一级（最高 L3）。
- **恢复**：连续 6 个采样窗口 `> 56fps` → 降一级（最低 L0）。
- **L3 额外策略**：若当前模式含 `line` / `constellation`（O(n²) 连线），临时换成轻量底图（`cosmic→star`、`minimal→dust`、`cyber→grid`、其它→`float`），并记住原模式；回到 L2 以下时恢复。
- **作用域**：只影响当次会话，不写 `localStorage`，刷新即恢复用户原本的选择。

### 其它各级降级

| 触发条件 | 行为 |
|---|---|
| `prefers-reduced-motion: reduce` | 只渲染一帧静态背景后直接 return，不启动 rAF 循环；活体光标、着陆动画、引力星轨同样跳过 |
| `navigator.connection.saveData` | `lowPerf = true` → 力场与点击动效默认关闭 |
| 触屏且窗口宽 ≤ 768 | 同上（用户显式开启则尊重其选择） |
| 电池电量 < 20% 且未充电 | 异步检测（`navigator.getBattery`），命中后重新读取开关并降级 |
| 窄屏 | DPR 基准上限 1.5（宽屏 2），再与 `perf.dprCap` 取小 |

调试钩子：`window.__bg.snapshot()` 返回当前粒子数、力场/点击开关、`perfLevel`、`fps`、`partFactor`、`dprCap` 等，便于自动化校验与排查。

## 五、客户端脚本模块清单

`BaseLayout.astro` 是全站唯一的脚本入口（除两个按页加载的模块外）：

| 模块 | 职责 |
|---|---|
| `bg.js` | 背景引擎：单 rAF 循环 + `REG` 注册表 + `A+B` 叠加 + 力场/点击叠加层 + FPS 自适应降级 |
| `fx.js` | 基础交互：滚动揭示、卡片跟随鼠标的环境光晕、返回顶部、阅读进度条、代码块头部（语言/文件名/复制）、目录滚动高亮 |
| `palette.js` | 配色工坊：预设（HSL 明暗自适应）、色轮自定义、随机、导入导出、双面板 UI 同步 |
| `cursor.js` | 活体光标：隐藏原生指针，DOM 双速跟随（环慢点快）+ Canvas 发光粒子拖尾；触屏 / reduced-motion 不启用 |
| `audio.js` | 空间音效：Web Audio + StereoPanner，按 `clientX` 计算声像，节流避免爆音，首次手势后 `resume` |
| `radar.js` | 深空雷达搜索：📡 按钮唤出扫描圈，按相关度把卡片牵引到中心；无结果时提示并推荐热门标签 |
| `warp.js` | 休眠舱与能量条：`initWarp()`（卡片克隆放大转场）已默认关闭，只启用 `initCryo()` 休眠逻辑 |
| `interactions.js` | 星际通讯：点赞 = 注入能量（matter-js 物理反馈），收藏 = 牵引光束（写入 `localStorage.favs`） |
| `console.js` | 成就与彩蛋：6 枚徽章（首次点赞 / 首次收藏 / 雷达 / 凌晨三点 / 读满 5 篇 / 换 3 套皮肤）+ 方向键彩蛋 |
| `visit-counter.js` | 访客计数上报：`load` 后 POST `/api/visitors` 携带 `location.pathname`；失败静默留空 |
| `landing.js` | 首屏着陆动画（同会话仅一次，`sessionStorage.fight1_landed`）；触屏 / reduced-motion 跳过 |
| `universe.js` | 核心调度器：全局 rAF Ticker（delta time）、CSS 变量读取、设备与无障碍探测、性能档位判定 |
| `orbits.js` | 引力星轨：仅 `/tags` 页按需加载，Canvas 引力约束 + 邻近连线，点击节点跳到该标签 |
| `stages.js` | 反重力卡片舞台：仅 `/notes` 页按需加载，GSAP + CSS 3D 在 grid/depth/fan/cylinder/sphere/spiral/wave 间平滑变形 |
| `ripple.js` | 点击涟漪：当前未被任何页面 import，属预留模块 |

## 六、PWA 层

`public/sw.js`（在 `BaseLayout` 中 `load` 后注册）采用保守策略，避免缓存脏数据：

| 请求类型 | 策略 |
|---|---|
| 页面导航（`mode === 'navigate'`） | **网络优先**，失败回落缓存，再兜底 `/` |
| 静态资源（`/_astro/*` 与 js/mjs/css/字体/图片/json） | **缓存优先 + 后台更新** |
| `/api/*`（Pages Function） | **不接管**，必须实时 |
| 跨域请求（giscus、Cloudflare beacon） | **不接管** |

- 缓存分 `core-v1`（安装时预缓存 `/`）与 `runtime-v1`；`activate` 时清理其它版本的缓存并 `clients.claim()`。
- 只处理 `GET` 请求。
- `public/manifest.webmanifest` 提供 `standalone` 显示模式、`#070a16` 背景、`#5eead4` 主题色与 SVG 图标。

## 七、后端层（`functions/api/visitors.js`）

全站唯一的动态端点，数据存在 KV 的**单键** `stats` 中：

```json
{ "pv": 1234, "uv": 567, "pages": { "/notes/agent/xxx": 42, "/": 88 } }
```

| 方法 | 行为 |
|---|---|
| `GET /api/visitors` | 返回 `stats`；未绑定 KV 时返回 `{pv:0,uv:0,pages:{},noKV:true}` |
| `GET /api/visitors?top=N` | 额外返回按 PV 降序的前 N 条路径（`N` 上限 20），供首页「热门文章」使用 |
| `POST /api/visitors` | PV +1；当天首次访问 UV +1；按路径累计 `pages[path]`（优先取 body 的 `path`，回退 `Referer`） |

- **UV 去重**：以**北京时间**（`UTC+8`）日期为准，命中后下发 `visitor_day=YYYY-MM-DD` Cookie（`Max-Age=86400; SameSite=Lax`），跨天自动重新计数。
- **机器人过滤**：UA 命中 `bot|crawl|spider|slurp|bingpreview|facebookexternalhit` 时只返回当前值、不计入。
- **体积控制**：路径数超过 300 时只保留 PV 最高的 150 条。
- 响应统一 `Cache-Control: no-store`。

## 八、SEO 层

| 项 | 实现 |
|---|---|
| `sitemap.xml` | `src/pages/sitemap.xml.ts` 自生成，排除 `console` / `favorites` / `drafts` 等个人页与草稿 |
| `robots.txt` | `src/pages/robots.txt.ts`，`Allow: /` + Sitemap |
| canonical | `BaseLayout` 输出 `<link rel="canonical" href={Astro.url.href}>` |
| OG / Twitter | `og:site_name/title/description/type/url/locale/image` + `twitter:card=summary_large_image`；`ogType` 文章页为 `article` |
| JSON-LD | 全站 `WebSite`；文章页追加 `Article`（headline/author/keywords/datePublished/image）；文章页追加 `BreadcrumbList`（首页 › 笔记 › 分类 › 本文） |
| 页面级 noindex | `BaseLayout` 的 `noindex` prop（`/drafts` 使用） |

**OG 分享图**：`scripts/gen-og.py` 用 Pillow 在本地把每篇非草稿文章的 frontmatter（标题 / 分类 / 摘要 / 日期）渲染成 **1200×630 PNG**，输出到 `public/og/<分类>-<短名>.png`。
文章页在**构建期**用 `fs.existsSync` 检查该文件是否存在，优先级为：

```
/og/<slug 中 / 换成 ->.png   >   文章 cover    >   /og-default.svg
```

因此新文章若未跑一次生成脚本，分享卡片会静默回退默认 SVG，不会报错。脚本幂等，可重复执行。

## 九、性能与缓存策略

`public/_headers` 随产物发布（CF 默认已开 Brotli/Gzip）：

| 匹配 | 响应头 |
|---|---|
| `/_astro/*`、`*.js`、`*.css`、`*.woff2`、`*.woff` | `public, max-age=31536000, immutable` |
| `*.svg` `*.png` `*.jpg` `*.jpeg` `*.webp` `*.gif` | `public, max-age=86400` |
| `/*`（全站） | `X-Content-Type-Options: nosniff`、`Referrer-Policy: strict-origin-when-cross-origin`、`Permissions-Policy: accelerometer=(), camera=(), geolocation=(), gyroscope=(), microphone=()` |

> 刻意**不设 CSP**：站点存在大量内联脚本（主题预设、giscus 注入、分享逻辑等），强 CSP 会破坏渲染。

其它性能取舍：
- 构建期零图片处理依赖（OG 图本地生成后作为静态资源提交），降低 CF 构建失败风险。
- 客户端依赖很轻：GSAP 与 matter-js 只用于卡片舞台和点赞物理反馈；背景、光标、雷达均为原生 Canvas。
- `preconnect` 到 `static.cloudflareinsights.com` 与 `giscus.app`；giscus 脚本默认不加载。

## 十、无障碍

- **跳转链接**：`<a class="skip-link" href="#main">跳到主内容</a>`，聚焦时可见。
- **焦点样式**：全局 `:focus-visible` 统一 2px 强调色描边；`.skip-link:focus` 有专门样式。
- **当前页标识**：`Nav.astro` 对命中路径的链接输出 `aria-current="page"`；所有按钮/控件均带 `aria-label`。
- **减少动效**：`prefers-reduced-motion: reduce` 时背景只渲染一帧，活体光标、着陆动画、引力星轨、点击涟漪全部跳过；`global.css` 另有两处 media query 关闭过渡与动画。
- **语义与状态**：阅读进度条 `aria-hidden`；邮件订阅提示 `role="status" aria-live="polite"`；代码块复制按钮有 `aria-label`。
- **打印样式**：`@media print` 精简输出。
