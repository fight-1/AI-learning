# 用户手册

面向两类人：只想读文章的**读者**，以及要往站点里写东西的**作者**（通常都是 fight-1 本人）。

---

## 第一部分 · 读者指南

### 1. 导航与页面一览

| 路径 | 页面 | 说明 |
|---|---|---|
| `/` | 首页 | 站点介绍、精选笔记、分类/标签入口、热门文章榜（按 KV 累计 PV） |
| `/notes` | 笔记列表 | 可按 `?category=xxx` / `?tag=xxx` 筛选；右上角可切换卡片舞台布局 |
| `/notes/<分类>/<短名>` | 文章详情 | 目录、阅读时长、上一篇/下一篇、相关阅读、分享栏、评论区 |
| `/archive` | 归档 | 全部文章按年份分组，时间倒序 |
| `/categories` | 分类 | 固定书架，显示每个分类的篇数 |
| `/tags` | 标签 | 两种视图：**文字云**（按热度字号）与**引力星轨**（Canvas 引力图，光标牵引、点击节点跳转） |
| `/favorites` | 收藏 | 本机收藏的文章列表 |
| `/console` | 舰长控制台 | 外观定制、配色工坊、探索成就、航行设置 |
| `/drafts` | 草稿舱 | 作者写作中的笔记（对搜索引擎 `noindex`） |
| `/search` | 搜索 | Pagefind 全文检索，支持 `?q=关键词` 直达 |
| `/subscribe` | 订阅 | RSS 地址复制、按分类订阅、邮件订阅（占位） |
| `/about` | 关于 | 站点与作者说明 |

导航栏右侧控件（从左到右）：📡 雷达搜索、`搜索`、⚙ 战甲面板（换肤 + 背景 + 配色）、☾/🖥/☀ 明暗切换。

### 2. 换肤 / 明暗 / 自动主题

- **换肤**：点导航栏「战甲」→ 选一套皮肤（星空 / 古风 / 赛博 / 玄幻 / 极简），同一面板里还能选该皮肤下的**背景模式**。也可以直接进 `/console` 的「外观定制」。
- **明暗**：点导航栏的月亮/屏幕/太阳图标，循环切换 **深色 → 浅色 → 自动**。
  - 图标含义：☀ 当前深色（点击切浅色）、☾ 当前浅色（点击切自动）、🖥 当前自动（跟随系统）。
  - 「自动」会实时响应系统的深浅色偏好变化，无需刷新。
- 所有选择记在本机 `localStorage`，下次打开自动恢复；换页不闪烁。

### 3. 配色工坊

入口：导航栏「战甲」面板底部，或 `/console` → 「🎨 配色工坊」。两处共享同一份状态。

| 操作 | 说明 |
|---|---|
| 配色预设 | 跟随皮肤 / 森林 / 海洋 / 琥珀 / 绛红 / 紫罗兰。预设只存色相与饱和度，**亮度随当前明暗自适应**——深色下更亮、浅色下压暗，保证任何皮肤都协调可读 |
| 色轮自定义 | 直接调主色；副色自动按「色相 +30°」派生，也可单独微调；拖动时实时预览，松手才写入本机 |
| 🎲 随机配色 | 随机生成一组协调配色（随机色相 + 0.55~0.85 饱和度），同样明暗自适应 |
| ⤓ 导出 | 下载 `palette.json`（含预设来源与三色值），便于备份或分享 |
| ⤒ 导入 | 选择 `palette.json` 恢复配色；也接受只含 `accent` 的裸 JSON |
| ↺ 跟随皮肤原色 | 清除自定义，回到该皮肤自带的配色 |

### 4. 背景模式与动态交互

- 在战甲面板按当前皮肤列出可选背景（如「星点 / 银河 / 流星雨 / 星座连线 / 星云 / 悬浮光块」），点选即时切换。
- `/console` → 「航行设置」可开关：

| 开关 | 作用 |
|---|---|
| 活体光标 + 粒子拖尾 | 关闭后恢复系统鼠标指针，立即生效 |
| 文章逐行浮现 | 跃迁打字机效果 |
| 粒子力场 | 鼠标对背景粒子施加**排斥**（默认）或**吸引**（勾下方「力场模式：吸引」），或关闭 |
| 鼠标点击动效 | 点击处的涟漪 / 火花粒子爆发 |
| 评论区 | 默认**关闭**。开启后文章页才懒加载 giscus（GitHub Discussions，需能访问 `giscus.app`） |
| 专注阅读模式 | 隐藏背景动效、收起目录侧栏、正文加宽至 720px；文章页顶部也有「专注阅读」按钮 |

- 系统开启「减少动态效果」时，全站动效自动降级为静态排版。
- 移动端、弱网省流量模式、电量低于 20% 时，力场与点击动效默认关闭；运行中掉帧也会自动降级（只影响本次会话）。

### 5. 专注阅读模式

文章页头部点「专注阅读」（或控制台里勾选）→ 背景画布隐藏、目录侧栏收起、正文居中加宽、评论区淡化。
状态存本机，跨页保持；再点一次「退出专注」即可。

### 6. 搜索

- **快捷键 `/`**：任意页面按 `/` 直接跳到 `/search` 并聚焦输入框（在输入框内输入时不触发）。
- 搜索基于 Pagefind 构建期索引，支持中文分词，覆盖标题 / 正文 / 标签。
- 另有导航栏 📡 **雷达搜索**：不跳页，直接在当前页唤出扫描圈，按相关度把卡片牵引到中心；无结果时推荐热门标签。
- 支持 `?q=关键词` 直达，如 `/search?q=MCP`。

### 7. 收藏

- 卡片或文章页的「☆ 收藏」按钮把文章加入收藏，列表在 `/favorites` 查看。
- 收藏数据只存在**本机浏览器**（`localStorage.favs`），不会上传，换设备/清缓存不保留。
- 文章页的「⚛ 注入能量」是点赞，同样记在本机（`localStorage.likes`），纯视觉反馈。

### 8. 成就系统

在 `/console` → 「探索成就」查看，解锁时页面底部会弹出提示。共 6 枚：

| 徽章 | 解锁条件 |
|---|---|
| ⚛ 首次注入能量 | 第一次点赞 |
| 🛰 首次牵引入库 | 第一次收藏 |
| 📡 深空探测者 | 第一次使用雷达搜索 |
| 🌙 凌晨三点航行 | 在凌晨 3 点访问站点 |
| 📖 阅读 5 篇 | 累计阅读 5 篇文章 |
| 🎭 形态切换者 | 切换过 3 套皮肤 |

另有一个彩蛋：连续输入 ↑ ↑ ↓ ↓ ← → ← →（或 `updownleftright`）触发全屏 Glitch。

---

## 第二部分 · 作者指南

### 1. 如何写文章

在 `src/content/notes/<分类>/<英文短名>.md` 新建文件（可从 `templates/note-template.md` 复制）。
目录名即分类，文件名短名决定 URL：`src/content/notes/agent/foo.md` → `/notes/agent/foo`。

```markdown
---
title: 文章标题
date: 2026-09-10
category: Agent
tags: [多智能体, 编排]
summary: 一句话摘要，显示在卡片、RSS 和分享卡片上。
featured: false
draft: false
---

# 正文起始

正文用标准 Markdown / MDX 即可。
```

**frontmatter 字段说明**

| 字段 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `title` | ✅ | — | 标题 |
| `date` | ✅ | — | 发布日期（`YYYY-MM-DD`，用于排序 / 归档 / OG 图） |
| `category` | ✅ | — | 分类名，出现在分类页、面包屑、OG 图角标 |
| `tags` | — | `[]` | 标签数组，用于标签页、相关阅读打分、搜索 |
| `summary` | — | `''` | 一句话摘要（建议 30~60 字），用于卡片 / RSS / `og:description` |
| `featured` | — | `false` | `true` 时卡片显示「精选」角标 |
| `draft` | — | `false` | **`true` 不发布**：列表、RSS、sitemap 全部过滤，只在 `/drafts` 可见 |
| `cover` | — | — | 可选封面图路径；OG 图缺失时作为回退 |

校验规则见 `src/content/config.ts`（Zod schema），字段写错会在构建时报错。

**写作小贴士**

- 代码块用围栏语法，可选加 `title`：```` ```ts title="src/main.ts" ````，前端会自动渲染语言标签、文件名与复制按钮。
- 正文中的 `##` / `###` 会自动进入右侧目录，并生成可点击复制的锚点 `#`。
- 写完先本地 `npm run dev` 看效果，确认后 `git commit && git push`。

### 2. 如何生成 OG 分享图

每篇文章的分享卡片（`og:image`）来自本地生成的 1200×630 PNG。

```bash
pip install Pillow      # 只需一次
npm run og              # 等价：python scripts/gen-og.py
```

- 脚本扫描 `src/content/notes/**/*.md`，跳过 `draft: true`，把 frontmatter 渲染成 `public/og/<分类>-<短名>.png`（幂等，可反复执行）。
- **新文章务必跑一次**，否则分享到微信 / X / QQ 时卡片会回退到全站默认的 `/og-default.svg`。
- 生成的 PNG 属于静态资源，需要**一起提交进仓库**。
- 字体优先取微软雅黑（`C:\Windows\Fonts\msyh.ttc` / `msyhbd.ttc`），缺失时回退 `simhei.ttf`，再缺失则用 Pillow 默认字体。

### 3. 如何重新生成搜索索引

无需手动操作：`npm run build` 里已经串了 `pagefind --site dist`，Cloudflare 每次构建都会重新生成 `/pagefind/*` 索引。

- 本地预览搜索要先 `npm run build` 再 `npm run preview`（`astro dev` 下 Pagefind 索引不存在，搜索页会提示「索引正在生成中」）。
- 若构建环境的 pagefind 失败，脚本以 `|| true` 兜底，不会让整站构建失败——但此时线上搜索不可用。

### 4. 如何部署

托管在 Cloudflare Pages，**推送到 `main` 分支即自动构建**，约 1~2 分钟上线。

```
1) 写文章 / 改代码
2) npm run og        （有新文章时）
3) git add . && git commit -m "..."
4) git push origin main
```

构建配置（首次已设好）：Framework `Astro` / Build command `npm run build` / Output `dist` / `NODE_VERSION=22`。
换自定义域名后，记得同步修改 `astro.config.mjs` 里的 `SITE` 常量（影响 RSS 与站内绝对链接、canonical、sitemap）。

### 5. 访客计数 KV 绑定步骤

页脚的「访客 N · 浏览 M」由 `functions/api/visitors.js` + KV 实现。**未绑定时计数器静默隐藏，不影响其它功能**；本地 `npm run dev` / `preview` 同样没有该端点。

1. Cloudflare 控制台 → `Workers & Pages` → 本项目 → `Settings` → `Functions` → **KV variable bindings** → `Add binding`
2. **Variable name** 填 `VISITOR_KV`（必须与代码里的 `env.VISITOR_KV` 完全一致），**Value** 选一个 KV namespace
3. 若还没有 namespace：先到 `Workers & Pages` → `KV` → `Create a namespace`（如 `ai-learning-visitors`），再回来绑定
4. 重新触发一次部署生效

计数口径：每次访问 PV +1；**按北京时间日期去重**，同一浏览器当天首次访问 UV +1（下发 `visitor_day` Cookie），跨天自动重新计数；爬虫 UA 不计入。

### 6. 邮件订阅现状

`/subscribe` 页的邮件订阅**目前是占位实现，未接入真实邮件服务**：

- 虚拟邮箱为 `subscribe@fight-1.example.com`（`.example.com` 是保留域名，实际不存在）。
- 当前提交表单只会提示「尚未接入真实邮件服务」，并尝试唤起访客的邮件客户端——**不会真的收到订阅请求，也不会保存邮箱**。
- 真正可用的订阅方式是 **RSS**：复制 `/rss.xml`（或按分类 `/rss.xml?category=xxx`）到任意阅读器。

**接入方法**：打开 `src/pages/subscribe.astro`，把脚本里的常量

```js
const SUBSCRIBE_ENDPOINT = '';
```

换成 Buttondown / Mailchimp / Formspree 等服务的表单地址（如 `https://buttondown.email/api/emails/embed-subscribe/xxx`）。
脚本已写好 POST 分支：填了地址就会 `fetch` 提交并在页面显示「订阅成功 / 订阅失败」，无需改其它逻辑。
